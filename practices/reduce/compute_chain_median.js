'use strict';

// typeof struct Collection
// {
//   var data;
//   struct collect *next;

// }node;

function compute_chain_median(collection) {
  //在这里写入代码
  // var median_data;
  // for(var i = 0; i < collection.length; i++){
  //   for(var j =0; j < collection.length-1-i; j++){
  //     if(collection)
  //   }
  // }
  // return median_data;
}

module.exports = compute_chain_median;
